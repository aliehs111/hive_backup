  <link rel="stylesheet" href="../public/css/style.css">

<ul id="hexGrid">
  {{#each events as |event| }}
    <li class="hex">
      <a class="hexIn" href="#">
        <img src="{{event.event_image}}" alt="">
        <h1>{{event.even_name}}</h1>
        <p>{{event.event_date}}</p>
      </a>
    </li>
  {{/each}}
</ul>


